# Docker: фронт + Node API на одном домене (оплата Prodamus).
# Переменные VITE_* — этап «Сборка»; TELEGRAM_*, PRODAMUS_*, SERVE_STATIC — «Запуск».
meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
run:
  containerPort: 8080
